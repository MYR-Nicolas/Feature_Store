name: "dbt_btc"
version: "1.0.0"
config-version: 2

profile: "dbt"

model-paths: ["models", "intermediate", "marts"]
macro-paths: ["macros"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt:
    intermediate:
      +schema: intermediate
      +materialized: view

    marts:
      +schema: feature_store
      +materialized: table

    models:
      +schema: staging
      +materialized: view